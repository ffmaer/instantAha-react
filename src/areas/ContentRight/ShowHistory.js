import React, { Component } from "react";

class ShowHistory extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <button>Show history</button>;
  }
}

export default ShowHistory;
